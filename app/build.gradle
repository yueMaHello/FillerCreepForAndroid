apply plugin: 'com.android.application'

android {
    compileSdkVersion 18

    defaultConfig {
        applicationId "es.softwareprocess.fillercreep"
        minSdkVersion 7
        targetSdkVersion 7
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
